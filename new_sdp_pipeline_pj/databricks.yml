# This is a Databricks asset bundle definition for new_sdp_pipeline_pj.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: new_sdp_pipeline_pj
  uuid: 0d46f88e-61c6-429b-903a-78b65da38450

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-f1d46cb2-e7aa.cloud.databricks.com
    variables:
      catalog: workspace
      schema: ${workspace.current_user.short_name}_sdp
  prod:
    mode: production
    workspace:
      host: https://dbc-f1d46cb2-e7aa.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/isanakamishiro@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/isanakamishiro@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: workspace
      schema: prod_sdp
    permissions:
      - user_name: isanakamishiro@gmail.com
        level: CAN_MANAGE
